# eBay Button Scout — Python dependencies
# Flask + gunicorn + slack-bolt power the daily scan and the /crawl500 slash
# command. PyTorch CPU-only to keep the image small.

--index-url https://download.pytorch.org/whl/cpu

torch==2.2.2+cpu
torchvision==0.17.2+cpu

--extra-index-url https://pypi.org/simple

# CLIP (from source — same as buybot and buttonmatcher)
clip @ git+https://github.com/openai/CLIP.git

# Computer vision
opencv-python-headless==4.11.0.86
pillow==12.2.0
numpy==1.26.4

# GCP clients
google-cloud-secret-manager==2.27.0
google-cloud-storage==3.10.1
google-auth==2.50.0

# Gemini (image triage for /crawl10)
google-genai==1.5.0

# Google Sheets
gspread==6.2.1

# Slack
slack-bolt==1.28.0
slack-sdk==3.41.0

# Web server
flask==3.1.3
gunicorn==25.3.0

# HTTP
requests==2.33.1
certifi==2026.4.22
charset-normalizer==3.4.7
idna==3.13
urllib3==2.6.3

# Misc
ftfy==6.3.1
regex==2026.4.4
tqdm==4.67.3
